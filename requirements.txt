# Core: training, masking strategies, zero-shot evaluation, tests, demo
torch>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0        # HuggingFace Trainer backend
rjieba                    # required by the RoFormer (AntiBERTa2) tokenizer
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
pytest>=7.0              # run the masking unit tests in tests/

# Data preparation (scripts/download_data.py, compute_*_labels.py, ...)
# Only needed to (re)build the full pretraining corpus + metadata sidecars;
# not required for the bundled demo or the unit tests.
pandas>=2.0.0
requests>=2.28
PyTDC>=1.0               # paratope / developability label sources (SAbDab, TAP)
